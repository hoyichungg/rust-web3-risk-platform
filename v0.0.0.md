做一個 「多鏈資產風險儀表板 + 策略回測/告警平台」：

使用者用錢包登入 → 匯總多鏈資產與倉位 → 顯示風險指標 → 提供簡單策略回測＆告警 → 角色權限可控（B2B SaaS skeleton）。

可以想像成：

「Web3 版的風險 EMS + 策略實驗室」

⸻

功能如何對應 2 / 3 / 4
	•	點子 4：Web3 登入 + 權限控管（骨架）
	•	用錢包登入（SIWE / 自訂簽名登入）
	•	合約儲存角色：Admin / Viewer
	•	後端（Rust）根據 on-chain role 控制 API 權限
→ 整個平台是 B2B SaaS 風格，而不是只是玩具 DApp。
	•	點子 3：多鏈資產監控 & 風險儀表板
	•	使用者可以綁定多個錢包地址（可能跨鏈）
	•	Rust indexer 抓每個地址的：ETH / ERC20 餘額、簡單 DeFi 倉位（可以先只支援一兩個協議或甚至只做「餘額＋市值」）
	•	算 TVL / LTV / health factor / 資產分布
	•	前端顯示成 Dashboard（Pie / Bar / Trend）
	•	點子 2：策略回測 +（未來）On-chain 執行 skeleton
	•	Rust 寫策略引擎（讀歷史價格 / 建倉紀錄，跑回測）
	•	回傳績效曲線、最大回撤、Sharpe 等
	•	可以在 UI 上「建立策略 → 跑回測 → 保存設定」
	•	進階版：加上一個「Execution Bot skeleton」，但先做到只產生 signal 就好（例如在 UI 顯示「今天建議減倉」）。

⸻

系統架構（高層）

1. Frontend（Next.js + MUI）
	•	/login：Connect Wallet + 簽名登入
	•	/dashboard：顯示
	•	總資產、市值走勢
	•	風險指標（LTV、health factor、敞口分布）
	•	/strategies：
	•	列出策略（名稱、標的、狀態）
	•	操作「跑回測」→ 顯示績效圖
	•	/alerts：
	•	設定告警條件，如「LTV > 70%」、「資產跌幅 > 10%」

⸻

2. Backend（Rust / Axum 或 Actix）

模組可以分成幾個 crate：
	•	auth 模組
	•	處理簽名登入（例如 EIP-4361 風格）
	•	向鏈上 RoleManager 合約查角色
	•	發 JWT / session token
	•	portfolio-indexer 模組
	•	定期掃描使用者白名單錢包
	•	透過 ethers-rs 或 RPC 獲取：
	•	ETH / ERC20 balance
	•	簡單 LP / 倉位（可先做最陽春版本）
	•	寫入 Postgres / Redis
	•	strategy-engine 模組
	•	讀歷史價格（CSV / DB / API）
	•	內建幾個策略：MA 交叉、固定再平衡等等
	•	回傳回測結果（equity curve、指標）
	•	alert-engine 模組
	•	定期計算每個使用者的風險指標
	•	滿足條件時：
	•	寫 DB log
-（未來）寄 email / 推送到 Telegram Bot

⸻

3. Smart Contracts（EVM on testnet）
	•	RoleManager.sol
	•	setRole(address, role)：Admin 管理
	•	getRole(address) view returns (uint8)：後端查詢
	•	（選配）StrategyConfig.sol
	•	用來存策略啟用狀態或綁定的 vault（可以先不做）

⸻

專案資料夾結構建議

rust-web3-risk-platform/
  contracts/
    RoleManager.sol
    hardhat.config.ts   # 或 foundry / forge 設定
  backend/
    Cargo.toml
    crates/
      api/              # Axum HTTP API
      auth/             # 簽名登入 + RBAC 整合
      indexer/          # 多鏈錢包資產抓取
      strategy_engine/  # 回測核心
      alert_engine/     # 告警邏輯
      domain/           # 共用 model、DTO
  frontend/
    package.json
    app/                # Next.js App Router
  infra/
    docker-compose.yml  # Postgres, Redis, local chain (anvil)
    migrations/         # SQL schema
  README.md


  DB / 資料模型（簡略）
	•	users
	•	id
	•	primary_wallet
	•	created_at
	•	wallets
	•	id
	•	user_id
	•	address
	•	chain_id
	•	roles
	•	user_id
	•	role (Admin / Viewer)  // 真實 role 還是以 on-chain 為準
	•	positions（或 portfolio_snapshots）
	•	wallet_id
	•	asset_symbol
	•	amount
	•	usd_value
	•	timestamp
	•	strategies
	•	id
	•	user_id
	•	name
	•	type (e.g. “MA_CROSS”)
	•	params (JSON)
	•	strategy_backtests
	•	id
	•	strategy_id
	•	from_date
	•	to_date
	•	result_json  // equity curve, metrics…
	•	alert_rules
	•	id
	•	user_id
	•	type (LTV_OVER, DROP_OVER, …)
	•	threshold
	•	enabled

⸻

開發 Milestone（讓你有一條路可以一路做完）

Phase 1：Web3 登入 + RBAC 骨架
	•	寫 RoleManager.sol，部署到 testnet（Sepolia 之類）
	•	Rust auth 模組 + API：
	•	GET /auth/nonce
	•	POST /auth/login（帶簽名）→ 回傳 JWT
	•	前端實作 Connect Wallet + 簽名登入畫面
	•	前端根據角色切換：Admin / Viewer 顯示不同 menu

👉 這一階段做完，你就有一個完整的「Web3 SaaS 骨架」。

⸻

Phase 2：多鏈資產監控
	•	建 wallets / positions 表
	•	Rust indexer：
	•	先支援單一鏈（例如 Ethereum testnet）+ 單一資產（ETH + 1~2 個 ERC20）
	•	寫一個簡單 scheduler （tokio + interval）
	•	/dashboard 顯示：
	•	目前總資產
	•	資產分布 pie chart
	•	最近 N 次 snapshot 的趨勢線

⸻

Phase 3：策略回測模組
	•	strategy_engine crate：
	•	定義 Strategy trait
	•	實作一個簡單策略（MA 交叉）
	•	從 CSV 調歷史價格資料
	•	API：
	•	POST /strategies 建立策略＋參數
	•	POST /strategies/{id}/backtest
	•	Frontend：
	•	Strategies 頁面：建策略、按「跑回測」、顯示績效圖

⸻

Phase 4：告警引擎
	•	alert_rules schema + CRUD API
	•	alert_engine 後台 job：
	•	讀 positions / portfolio snapshots
	•	評估「TVL 跌幅」、「單一幣種占比過高」之類
	•	UI 顯示告警列表
	•	（未來）串 Email / Telegram 等實際通知


技術組合 Axum + Postgres + ethers-rs + Hardhat + Next 15 + Docker